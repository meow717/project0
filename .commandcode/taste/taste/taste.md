# Taste
- After a plan is produced and presented, the user prefers to kick off implementation with a minimal, terse command (e.g., "yes" to a "continue M1→M8?" prompt) and let the agent execute the full milestone sequence autonomously — verifying, testing, and summarizing at the end without intermediate check-ins. Fresh evidence: user approved a full 8-milestone backend+frontend run with a single word and gave no further input throughout. Confidence: 0.85
- Prefers provider-neutral ports with stub/no-op adapters in dev over integrating paid third-party SDKs prematurely; defers concrete provider choice to prod (chose "port + provider stub" for SMS over a full Twilio integration or dropping SMS). Confidence: 0.7
- Prefers incremental gap-closing over rebuilding what already works — approved a plan whose headline is "~80% built, close the specific gaps, do not rebuild what works." Confidence: 0.6
- Values code that avoids redundancy and catches dead-end designs mid-flight: when the agent realized `ReissueTokens` would be byte-for-byte identical to the existing `RefreshSession` and reverted it, the user raised no objection; the agent likewise skipped installing an optional new dependency (reactbits) when an existing component already covered the need. Confidence: 0.5
- Wants `.env.example` kept as a complete, ready-to-apply template — every variable documented, grouped by section with comments, and kept in sync with the actual settings keys (asked to "rewrite the .env.example so i can apply" after new env vars were added). Confidence: 0.6
- The user applies environment/secret config to `.env` himself; the agent's job is to keep `.env.example` updated, not to touch `.env` (request was explicitly framed as "so i can apply"). Confidence: 0.5
- Wants the shared API client to handle non-JSON bodies natively: prefers a single fetch wrapper that passes `FormData` through unmodified (browser sets the multipart boundary) instead of manual `Content-Type: multipart/form-data` headers — the agent retrofitted `lib/api-client.ts` accordingly for logo uploads. Confidence: 0.4
- Prefers custom brand-color themes over the stock light/dark black-and-white toggle: explicitly asked to replace Light/Dark with a yellow-gradient theme (default, dark text, white buttons) and a rich purple-gradient theme (bright readable text on dark), wired through the theme system's CSS variables — not just tweaking the existing dark mode. Fresh evidence: later refers to "our high-contrast design system (Purple and Yellow accents)" as the project's identity and asks every UI element to match it. Confidence: 0.75
- Wants smooth animated transitions when switching themes (e.g., a ~0.6s color/background fade) rather than an instant hard switch; explicitly asked that the sun/moon toggle "switch smoothly" between the custom themes. Confidence: 0.6
- Prefers short, catchy marketing copy on landing pages — explicitly asked to cut a long descriptive subtitle down to a two-word tagline ("وداعا للانتظار" / "Goodbye to Waiting") rather than keeping a paragraph of feature text. Confidence: 0.6
- Prefers landing/hero content centered in the exact middle of the screen (title, tagline, and CTAs vertically+horizontally centered), with secondary controls like language/theme toggles floated to a corner rather than in the flow. Confidence: 0.55
- Wants accessibility-grade text contrast in the custom themes, and names exact hex values to enforce it: dark charcoal `#1A1A1A` for all text/titles/button labels on the yellow theme, pure white `#FFFFFF` (or bright neon) on the purple gradient — muted/subtle token values (e.g., dark-brown muted text, lavender-tinged foreground) are rejected as insufficient. Fresh evidence: explicitly asked to ensure all elements "match our high-contrast design system." Confidence: 0.8
- When asking for styling/theme fixes, expects the change to be scoped to colors only — explicitly "keep the content, buttons, and layout exactly as they were"; restyling should not alter layout, component structure, or functionality. Confidence: 0.6
- Reports bugs/follow-ups as terse, detail-free one-liners (e.g., "still same problem"; "when i login i cant see the browse buinesses why") with no repro info or screenshots, and expects the agent to independently dig in, diagnose the root cause, and verify the fix against the actual built output rather than asking clarifying questions. Fresh evidence: asked "why" a page was missing after login with no context; the agent traced routing, found the nav gap, fixed it, and type-checked. Also reported an "auto-login on browse" bug with no repro steps; the agent traced the auth flow across frontend and backend, fixed `tryRefresh()` in the api client, and type-checked. Confidence: 0.7
- Wants logout to be definitive: the app must never silently re-authenticate a logged-out user (a background token refresh resurrecting a session from a stored refresh token was reported as a bug — "it login in automaticaly as a user but i should not have"). Public surfaces like the browse directory must be usable while logged out, with login prompted only when an action genuinely requires it. Confidence: 0.6
- Also gives feature requests as terse one-liners that pin down exact placement and exact values (e.g., chips "right below the search bar" with a precise list of Arabic labels; a card grid "below the filters" with an exact list of required card contents — name, area tag, waiting count, bilingual ticket button), leaving the agent to locate the relevant code itself and implement across the full stack without further clarification. Fresh evidence: asked to "add this futher in browse bunisses" with no detail beyond the target page; later requested the identity strip "in a bar in left corner in a little bar plus add logout option next to signed in" with placement and adjacent controls specified but no implementation detail. Fresh evidence: "i want you to remove \"demo\" word and leave coffe word" is another terse one-liner pinning the exact desired outcome with no further clarification. Fresh evidence: asked for an identity/guest icon "next to translate icon" on business cards with placement and behavior pinned down and no further clarification. Confidence: 0.75
- Change requests can be loose and approximate — e.g., "change the business card of demo clinic into lets say coffe" — where the agent is expected to choose the concrete details itself (business name, slug, services, category), keep every related surface consistent (it added a "مقاهي" category to the browse filter chips so the new shop was browsable), and apply the change to the running environment (re-ran the idempotent seed command against the local DB so the user sees the result immediately on refresh) without asking clarifying questions. Confidence: 0.5
- Wants the current authentication identity made explicit on public pages: the browse directory should show who you are — the signed-in user's name/email (and role like "Staff") or a clear "browsing as guest" notice with a login CTA — so the user can always tell at a glance whether they're logged in or a guest ("it aint show me who am i like am i a new user (not login) or its me"). Fresh evidence: asked for a little icon next to the translate icon on business cards that shows "the identity or guest", extending the identity/guest explicitness to business detail pages, not just the browse list. Fresh evidence: the icon-only version wasn't enough — asked to put the user's name (or the word "guest") as visible text right next to the identity icon, so the status is readable at a glance rather than hidden in a tooltip. Confidence: 0.8
- Expects every new user-facing string to be bilingual (Arabic + English) and added to both i18n locales — requests specify labels in both languages (e.g., a "حجز دور / Get Ticket" button), and the agent wires en/ar keys for each new label. Confidence: 0.55
- Wants live operational data (current waiting count) surfaced directly on directory/list cards so users see real-time status at a glance, not only on detail pages — cards read from the live snapshot and show a loading fallback until it arrives. Confidence: 0.4
- Expects every UI surface to consistently apply the brand Purple/Yellow accents with high contrast, and to be responsive and presentation-ready — asked to "ensure all elements match our high-contrast design system (Purple and Yellow accents) and look highly responsive and suitable for presentation"; the agent applied theme-matched accents to cards, chips, search, hover states, and empty/loading states across the browse surface. Confidence: 0.6
- Wants the language/translation toggle reachable on every page the user can navigate to, not only the main menu/landing — explicitly asked to add the translation option "whenever i go" after noticing it only appeared in the main menu; the agent added `DirectionToggle` to the public browse list and business detail pages (top corner). Confidence: 0.7
- Wants a persistent home button that returns to the main landing page, sitting in the same top control cluster as the identity badge and language toggle on every page — "also add homepage button to return the user to the main page" was a terse follow-up once those controls existed, and the agent added it to both the browse list and business detail pages (linking to `/`). Confidence: 0.6
- Prefers private, secure networking to share a running dev site with another machine: chose Tailscale (mesh VPN on both PCs) over public tunnels (ngrok) or cloud deployment, and expects concrete steps for that path rather than re-explanations of other options. Confidence: 0.5
- For "how do I run/access the site elsewhere" questions, wants concrete numbered instructions tailored to the project's actual configuration — the agent should inspect the real settings/env files (Django DEBUG/ALLOWED_HOSTS/CORS, frontend `NEXT_PUBLIC_API_URL`, which `.env` files are gitignored) and state exactly what is already enabled vs. what must change, including gotchas, rather than generic networking advice. Confidence: 0.55
- Is new to development concepts generally (not just deployment) and learns by asking basic conceptual clarifying questions ("is GitHub a real hosting?", "what is the meaning of demo why is it by default?"); responds well to plain-language explanations that map each term to their actual stack and clarify common conventions (e.g., demo = sample data seeded via a manual management command, not automatic on server start) before committing to a path. Fresh evidence: asked what the "Loading" vs "0 waiting" states on a business card mean — a basic UI/data question about live waiting counts — and the agent explained the card's live-count behavior in plain terms. Fresh evidence: re-asked "tell me again how to put this on cloud in github" — needs repeat, plain-language deployment walkthroughs and conflates GitHub with hosting, so the GitHub-vs-cloud-host distinction has to be re-explained. Fresh evidence: asked what git commit means ("how to committed ? you mean update the rpject right ?") — a basic git concept — and responded to the agent's plain-language commit/push explanation (snapshot + upload analogy) by confirming "yes, exactly." Confidence: 0.75
- Wants user-visible names/data to read as a real product, not placeholder demo content — explicitly asked to remove the word "demo" from the visible business name (leaving just "Coffee") so the public-facing card is clean, and expects the rename applied live to the running environment via the seed command. Confidence: 0.5
- Data-level changes must be minimal and side-effect-free: when the user asked only to remove the word "demo" from the business card, successive seed runs had left duplicate rows ("Demo Coffee" + "Coffee") and the user flagged the unexpected extra cards ("wait where there is 3 business cards i just wanted to remove demo word from the business card"); the agent cleaned up the stale duplicate and the user expects the running DB to show exactly the intended result with no leftover rows. Confidence: 0.55
